# Video Enhancement Requirements

# Python packages for video processing (system ffmpeg already installed)
moviepy>=1.0.3
opencv-python>=4.5.0

# Text-to-speech (optional - uses system TTS by default)
gTTS>=2.3.0
pydub>=0.25.0

# Audio processing
librosa>=0.9.0
soundfile>=0.10.0

# Image processing for thumbnails
Pillow>=9.0.0

# Progress tracking
tqdm>=4.64.0

# Additional video processing
imageio>=2.25.0
imageio-ffmpeg>=0.4.8